<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.hotel.hms.persistence.ShareDAO">
	
	<!-- 근무 정보 가져오는 메서드 -->
	<select id="getWork" parameterType="java.util.Map" resultType="com.hotel.hms.vo.WorkVO" >
		SELECT skdworkd, empCode FROM work_tbl_test WHERE skdMonth=#{skdMonth} AND empCode = #{empCode}
	</select>
	
	<!-- 직원정보 가져오는 메서드  -->
	<select id="getInfoEmp" parameterType="String" resultType="com.hotel.hms.vo.EmployeeVO">
		SELECT empCode, empName FROM emp_tbl WHERE empCode = #{empCode}
	</select>
	
	<!-- 매니저가 작성한 스케줄근무 db에 집어넣는 매핑 시퀀스 나중에 고치기 -->
	<insert id="insertSchedule" parameterType="map">
		INSERT INTO work_tbl_test (skdCode, skdMonth, empCode, skdWorkd)  VALUES (scdcode_seq.nextval, #{skdMonth}, #{empCode}, #{work}) 
	</insert>
	
	<insert id="insertDayOff" parameterType="java.util.Map">
		INSERT INTO holiday_tbl (holidayCode, empCode, startHoliday, endHoliday, state, reasonHoliday) VALUES(dayoff_seq.nextval, #{empCode}, #{startDate} ,#{endDate} , 1, #{dayOffReason})
	</insert>	
	
	<!-- 직원이 휴무 신청한 내역 가져오기 -->
	<select id="getLogDayOff" parameterType="String" resultType="com.hotel.hms.vo.HolidayVO">
		SELECT * FROM holiday_tbl WHERE empCode=#{empCode}		
	</select>
	
	<select id="checkWorkData" parameterType="map" resultType="int">
		SELECT COUNT(*) FROM work_tbl_test WHERE empCode=#{empCode} AND skdMonth= #{skdMonth} 
	</select>
	

	<!-- 직원정보 가져오는 메서드  -->
	<select id="getEmpInfo" parameterType="String" resultType="com.hotel.hms.vo.EmployeeVO">
		SELECT * FROM emp_tbl WHERE empName = #{empName}
	</select>

	
	
</mapper> 
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    